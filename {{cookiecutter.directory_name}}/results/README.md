# Results directory

This is the main directory for storing results from the analysis / pipeline.
By default, this repository is not synced to GitHub (controlled via `.gitignore`)
Main display items for manuscript (figures / tables) should be placed in the top-level `figures` & `tables` directories.

Use this README to describe directory structure and contents.