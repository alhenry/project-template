# Job Logs directory

This is the directory for storing log files for job submission.
`stderr` and `stdout` resulting from job submission (_ad hoc_ or via workflow manager) should be redirected here

By default, this repository is not synced to GitHub (controlled via `.gitignore`)

Use this README to describe directory structure and contents.